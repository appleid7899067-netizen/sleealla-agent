{
  "name": "sleealla-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Static Thai AI agent with 400 AI and self-development skills",
  "scripts": {
    "start": "python3 -m http.server ${PORT:-10000} --directory ."
  },
  "engines": {
    "node": ">=18"
  }
}
